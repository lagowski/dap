# Build artefacts — regenerated inside the multi-stage build, so
# pulling local copies into the context just slows the build and
# busts caches.
**/node_modules
**/.next
**/.venv
**/__pycache__
**/*.pyc
**/.pytest_cache
**/.mypy_cache
**/.ruff_cache

# Local state — never want a dev SQLite + LangGraph checkpoint
# database baked into the image.
.dap/
**/state.db
**/state.checkpoints.db
*.db

# Test / dev artefacts that don't belong in the runtime image.
tests/
docs/
examples/
.github/
.husky/
.changeset/
scripts/
.git/
.gitignore
.editorconfig
.coverage*
coverage/
htmlcov/

# IDE / OS noise.
.vscode/
.idea/
.DS_Store
Thumbs.db

# Secrets / local config.
.env
.env.*
!.env.example

# Markdown — README/CHANGELOG land in the GitHub Release, not the image.
*.md
!README.md

# Lockfiles for tooling we don't ship.
.tool-versions
