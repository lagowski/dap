// Shared test fixtures (constants used by multiple specs). Kept outside
// tests/ so Playwright doesn't try to load it as a test file.

export const FIXTURE_EMAIL = 'e2e-fixture@example.com';
export const FIXTURE_PASSWORD = 'FixturePassword123!';
