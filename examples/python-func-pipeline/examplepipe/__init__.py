"""Minimal example of a DAP ``python-func`` pipeline package.

This is the same shape Cortex uses: a small Python package of node functions,
installed into the engine's venv, referenced from a bundle JSON by
``module:function`` callable paths. See ``README.md`` for the full walkthrough.
"""
