[project]
name = "examplepipe"
version = "0.1.0"
description = "Minimal example of a DAP python-func pipeline package"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["examplepipe"]
