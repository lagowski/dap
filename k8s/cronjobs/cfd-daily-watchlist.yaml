apiVersion: batch/v1
kind: CronJob
metadata:
  name: cfd-daily-watchlist
  namespace: trading
spec:
  schedule: "0 6 * * 1-5"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
          - name: cfd-research
            image: ghcr.io/dixter999/cfd-research:latest
            args: ["cfd.sweep", "--watchlist", "daily"]
            envFrom:
            - secretRef:
                name: cfd-research-env
            resources:
              requests:
                memory: "512Mi"
                cpu: "250m"
              limits:
                memory: "1Gi"
                cpu: "500m"
          imagePullSecrets:
          - name: ghcr-pull-secret
